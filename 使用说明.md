# 本机小游戏 · 网页版使用说明

这个网页版不需要安装 App Store、不需要 Apple ID，也不需要签名。它可以在 iPhone 的 Safari 里直接玩，也可以“添加到主屏幕”像 App 一样打开。

## 一、先把文件放到手机能访问的地方

网页版包含很多文件（`index.html`、`index.wasm`、`index.pck` 等），不能直接双击 `index.html` 打开，需要先放到一个网址下面。二选一：

### 方式 A：免费发布到 GitHub Pages（推荐，可离线）

1. 注册并登录 [GitHub](https://github.com/)。
2. 新建一个仓库，例如 `local-games`，设为 **Public**（公开仓库可免费开 Pages；私密仓库需要付费）。
3. 把 `本机小游戏-网页版` 文件夹里的全部文件上传到仓库根目录。
4. 打开仓库的 **Settings → Pages**。
5. 在 **Branch** 里选择 `main` 和根目录 `/ (root)`，点 **Save**。
6. 稍等 1～2 分钟，页面顶部会出现一个网址，例如：

   `https://你的用户名.github.io/local-games/`

7. 在 iPhone 的 Safari 中打开这个网址，就能玩。

### 方式 B：同一 Wi-Fi 下，用 Mac 临时当作服务器

适合马上试玩，Mac 关机后就不能再访问。在 Mac 的“终端”里执行：

```bash
cd "/Users/mengfanli/Documents/Codex/2026-08-19/w/outputs/本机小游戏-网页版"
python3 -m http.server 8000
```

然后查看 Mac 的局域网 IP，例如“系统设置 → Wi-Fi → 详细信息”里的 IP，常见是 `192.168.x.x`。在 iPhone 的 Safari 打开：

```text
http://你的Mac局域网IP:8000
```

注意：方式 B 是 `http://`，iOS 可能无法让它完全离线缓存；建议正式使用时选择方式 A（`https://` 才能正常启用离线缓存）。

## 二、在 iPhone 上添加到主屏幕

1. 用 Safari 打开游戏网址。
2. 点底部的 **分享按钮**（方框带向上箭头）。
3. 往下滑，选择 **添加到主屏幕**。
4. 名称可以改成“本机小游戏”，点右上角 **添加**。
5. 回到主屏幕，点这个图标就能像 App 一样全屏打开。

首次联网打开一次后，服务人员会缓存游戏文件；之后即使暂时断网，一般也能再次打开。

## 三、和另一台电脑同步代码

推荐用 Git + GitHub 做版本同步。这样两台电脑都从同一个仓库拉取和推送，谁改了都能合并。

```bash
# 在项目目录里初始化（只需做一次）
cd /Users/mengfanli/Desktop/outputs
git init
git add .
git commit -m "导入本机小游戏"

# 关联你新建的仓库（把地址换成你自己的）
git remote add origin https://github.com/你的用户名/local-games.git
git branch -M main
git push -u origin main
```

另一台电脑上：

```bash
git clone https://github.com/你的用户名/local-games.git
```

之后每次改完：

```bash
git add .
git commit -m "说明这次改了什么"
git push
```

另一台电脑拉取最新：

```bash
git pull
```

如果两边同时改了同一处，Git 会提示冲突，按提示解决即可。
